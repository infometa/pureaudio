# 默认开启功能审计报告

## 问题描述

用户反馈：**环境自适应没开，但仍有抽吸感**

需要检查：
1. 哪些功能默认是开启的
2. 哪些功能在 UI 上没有开关按钮
3. 这些功能可能导致抽吸感

---

## 1. 默认开启的功能列表

### 1.1 在 `capture.rs` 中默认开启的功能

**位置**: `capture.rs:869-888`

```rust
let mut highpass_enabled = true;           // ✅ 高通滤波器
let mut transient_enabled = true;          // ✅ 瞬态增强
let mut saturation_enabled = true;         // ✅ 饱和器
let mut agc_enabled = true;                // ✅ AGC（自动增益控制）
let mut exciter_enabled = true;            // ✅ 谐波激励
let mut spec_enabled = true;               // ✅ 频谱推送
let mut rt60_enabled = true;               // ✅ RT60 计算
let mut final_limiter_enabled = true;      // ✅ 最终限幅器
```

**默认关闭的功能**：
```rust
let mut aec_enabled = false;               // ❌ AEC（回音消除）
let mut _timbre_enabled = false;           // ❌ 音色修复
let mut bypass_enabled = false;            // ❌ 旁路
let mut vad_enabled = false;              // ❌ VAD（语音活动检测）
let mut env_auto_enabled = false;          // ❌ 环境自适应
```

### 1.2 动态 EQ 的默认状态

**位置**: `audio/eq/dynamic_eq.rs:51-55`

```rust
pub fn new(sample_rate: f32, preset: EqPresetKind) -> Self {
    let mut eq = Self {
        enabled: true,  // ✅ 默认开启
        // ...
    };
}
```

**关键发现**：
- 动态 EQ **默认开启**（`enabled: true`）
- 但在 `capture.rs:1717-1719` 中，**没有检查 `dynamic_eq.enabled`**，直接调用 `process_block`
- `process_block` 内部会检查 `enabled`，但如果没有 UI 开关，用户无法关闭

---

## 2. UI 开关检查

### 2.1 有 UI 开关的功能

| 功能 | UI 开关 | 位置 |
|------|---------|------|
| 高通滤波器 | ✅ 有 | `main.rs:2700` |
| 瞬态增强 | ✅ 有 | `main.rs:2514-2526` |
| 饱和器 | ✅ 有 | `main.rs:2704` |
| AGC | ✅ 有 | `main.rs:2711` |
| 谐波激励 | ✅ 有 | `main.rs:2480-2485` |
| 最终限幅器 | ✅ 有 | `main.rs:2715-2723` |
| AEC | ✅ 有 | `main.rs:2702` |
| 音色修复 | ✅ 有 | `main.rs:2507-2512` |
| 环境自适应 | ✅ 有 | `main.rs:2725` |
| VAD | ✅ 有 | `main.rs:2726` |
| 动态 EQ | ✅ 有 | `main.rs:2173-2182` |

### 2.2 没有 UI 开关的功能

| 功能 | 默认状态 | 问题 | 位置 |
|------|---------|------|------|
| **频谱推送** (`spec_enabled`) | ✅ 开启 | ⚠️ **无 UI 开关** | `capture.rs:886` |

**注意**：
- `spec_enabled` 在 UI 中对应 `show_spectrum`，但这是**显示开关**，不是**处理开关**
- `rt60_enabled` 在 UI 中有开关（`main.rs:2739`），在频谱面板中

---

## 3. 可能导致抽吸感的功能

### 🔴 **PUMP-CRITICAL-001: 动态 EQ 默认开启且可能无 UI 控制**

**位置**: `capture.rs:1717-1719`

**问题**:
```rust
// 动态 EQ
let metrics = if let Some(buffer) = outframe.as_slice_mut() {
    dynamic_eq.set_dry_wet(1.0);
    dynamic_eq.process_block(buffer)  // <--- 没有检查 enabled
} else {
    EqProcessMetrics::default()
};
```

**分析**:
- 动态 EQ **默认开启**（`enabled: true`）
- 虽然 `process_block` 内部会检查 `enabled`，但如果没有 UI 开关，用户无法关闭
- 动态 EQ 的每个频段都有独立的增益控制，如果攻击/释放时间设置不当，可能导致增益快速变化，产生抽吸感

**检查 UI**:
- `main.rs:2173-2182` 有 `Message::EqEnabledChanged`，说明**有 UI 开关**
- 但需要确认用户是否知道可以关闭动态 EQ

**优先级**: **高**

---

### 🟡 **PUMP-MEDIUM-001: AGC 默认开启且响应过快**

**位置**: `capture.rs:1750-1752`, `audio/agc.rs:18-23`

**问题**:
```rust
if agc_enabled {
    agc.process(buffer);  // WebRTC AGC，无法调整攻击/释放时间
}
```

**分析**:
- AGC **默认开启**（`agc_enabled = true`）
- WebRTC AGC 的 `compression_gain_db: 20` 很强，会快速补偿低音量
- **无法调整攻击/释放时间**（`set_attack_release` 是空的）
- 当输入电平快速变化时，AGC 会快速响应，可能导致抽吸感

**UI 开关**: ✅ 有（`main.rs:2711`）

**优先级**: **中**

---

### 🟡 **PUMP-MEDIUM-002: 瞬态增强默认开启**

**位置**: `capture.rs:1711-1713`

**问题**:
```rust
if transient_enabled {
    if let Some(buffer) = outframe.as_slice_mut() {
        transient_shaper.process(buffer);
    }
}
```

**分析**:
- 瞬态增强 **默认开启**（`transient_enabled = true`）
- 瞬态增强会调整音频的起音和衰减，如果设置不当，可能导致音量快速变化

**UI 开关**: ✅ 有（`main.rs:2514-2526`）

**优先级**: **中**

---

### 🟡 **PUMP-MEDIUM-003: 饱和器默认开启**

**位置**: `capture.rs:1725-1727`

**问题**:
```rust
if saturation_enabled {
    if let Some(buffer) = outframe.as_slice_mut() {
        saturation.process(buffer);
    }
}
```

**分析**:
- 饱和器 **默认开启**（`saturation_enabled = true`）
- 饱和器会引入谐波失真，如果驱动过高，可能导致音量变化

**UI 开关**: ✅ 有（`main.rs:2704`）

**优先级**: **中**

---

### 🟡 **PUMP-MEDIUM-004: 谐波激励默认开启**

**位置**: `capture.rs:1728-1732`

**问题**:
```rust
if exciter_enabled {
    if let Some(buffer) = outframe.as_slice_mut() {
        exciter.process(buffer);
    }
}
```

**分析**:
- 谐波激励 **默认开启**（`exciter_enabled = true`）
- 谐波激励会增加高频内容，如果混合比例过高，可能导致音量变化

**UI 开关**: ✅ 有（`main.rs:2480-2485`）

**优先级**: **中**

---

### 🟢 **PUMP-LOW-001: 频谱推送默认开启**

**位置**: `capture.rs:1959-1967`

**问题**:
```rust
if spec_enabled && spec_push_counter % SPEC_PUSH_INTERVAL == 0 {
    if let Some((ref mut s_noisy, ref mut s_enh)) = s_spec.as_mut() {
        push_spec(df.get_spec_noisy(), s_noisy);
        push_spec(df.get_spec_enh(), s_enh);
    }
}
```

**分析**:
- 频谱推送 **默认开启**（`spec_enabled = true`）
- 虽然已节流（每 3 帧一次），但仍有性能开销
- **不会直接导致抽吸感**，但可能影响性能

**UI 开关**: ⚠️ **无**（只有显示开关 `show_spectrum`）

**优先级**: **低**

---

### 🟢 **PUMP-LOW-002: RT60 计算默认开启**

**位置**: `capture.rs:1366-1381`

**问题**:
```rust
if !is_voice && rt60_enabled {
    // RT60 计算
}
```

**分析**:
- RT60 计算 **默认开启**（`rt60_enabled = true`）
- 虽然只在非语音段计算，但仍有性能开销
- **不会直接导致抽吸感**，但可能影响性能

**UI 开关**: ✅ 有（`main.rs:2739`，在频谱面板中）

**优先级**: **低**

---

## 4. 关键发现

### 4.1 默认开启且可能导致抽吸感的功能

1. **动态 EQ** - 默认开启，有 UI 开关，但用户可能不知道可以关闭
2. **AGC** - 默认开启，有 UI 开关，但响应过快
3. **瞬态增强** - 默认开启，有 UI 开关
4. **饱和器** - 默认开启，有 UI 开关
5. **谐波激励** - 默认开启，有 UI 开关

### 4.2 没有 UI 开关的功能

1. **频谱推送** (`spec_enabled`) - 默认开启，**无 UI 开关**（只有显示开关 `show_spectrum`）

### 4.3 最可能导致抽吸感的功能

1. **动态 EQ** - 每个频段都有独立的增益控制，如果攻击/释放时间设置不当，可能导致增益快速变化
2. **AGC** - 响应过快，无法调整攻击/释放时间
3. **瞬态增强** - 会调整音频的起音和衰减，如果设置不当，可能导致音量快速变化

---

## 5. 建议

### 5.1 立即检查

1. **检查动态 EQ 是否开启**
   - 在 UI 中找到动态 EQ 开关，确认是否开启
   - 如果开启，尝试关闭，看是否还有抽吸感

2. **检查 AGC 设置**
   - 确认 AGC 是否开启
   - 如果开启，尝试调整 `compression_gain_db` 或关闭 AGC

3. **检查瞬态增强设置**
   - 确认瞬态增强是否开启
   - 如果开启，尝试关闭或调整参数

### 5.2 添加缺失的 UI 开关

1. **添加频谱推送开关**
   - 在 UI 中添加 `spec_enabled` 的开关
   - 允许用户关闭频谱推送，减少性能开销

2. **添加频谱推送开关**
   - 在 UI 中添加 `spec_enabled` 的开关
   - 允许用户关闭频谱推送，减少性能开销

### 5.3 优化默认设置

1. **降低 AGC 响应强度**
   - 将 `compression_gain_db` 从 20 降低到 12
   - 或者默认关闭 AGC，让用户手动开启

2. **优化动态 EQ 默认设置**
   - 检查动态 EQ 的攻击/释放时间设置
   - 如果过快，调整为更慢的值

---

## 6. 诊断步骤

### 步骤 1: 关闭所有可能的功能

1. 关闭动态 EQ
2. 关闭 AGC
3. 关闭瞬态增强
4. 关闭饱和器
5. 关闭谐波激励
6. 只保留降噪和高通

**如果还有抽吸感**，问题可能在：
- 降噪算法本身
- 高通滤波器
- 最终限幅器

### 步骤 2: 逐个开启功能

1. 开启降噪
2. 开启高通
3. 开启 AGC（观察是否有抽吸感）
4. 开启动态 EQ（观察是否有抽吸感）
5. 开启瞬态增强（观察是否有抽吸感）
6. 开启饱和器（观察是否有抽吸感）
7. 开启谐波激励（观察是否有抽吸感）

**这样可以定位具体是哪个功能导致的抽吸感**

---

## 7. 总结

### 7.1 默认开启的功能

- ✅ 高通滤波器（有 UI 开关）
- ✅ 瞬态增强（有 UI 开关）
- ✅ 饱和器（有 UI 开关）
- ✅ AGC（有 UI 开关）
- ✅ 谐波激励（有 UI 开关）
- ✅ 最终限幅器（有 UI 开关）
- ✅ 动态 EQ（有 UI 开关，但用户可能不知道）
- ⚠️ 频谱推送（**无 UI 开关**）
- ✅ RT60 计算（有 UI 开关，在频谱面板中）

### 7.2 最可能导致抽吸感的功能

1. **动态 EQ** - 每个频段都有独立的增益控制
2. **AGC** - 响应过快，无法调整攻击/释放时间
3. **瞬态增强** - 会调整音频的起音和衰减

### 7.3 建议

1. **立即检查动态 EQ 是否开启**，如果开启，尝试关闭
2. **检查 AGC 设置**，如果开启，尝试关闭或调整参数
3. **逐个关闭功能**，定位具体是哪个功能导致的抽吸感
4. **添加缺失的 UI 开关**（频谱推送、RT60 计算）

---

## 8. 相关代码位置

- **默认开启功能**: `capture.rs:869-888`
- **动态 EQ 处理**: `capture.rs:1717-1719`
- **AGC 处理**: `capture.rs:1750-1752`
- **瞬态增强处理**: `capture.rs:1711-1713`
- **饱和器处理**: `capture.rs:1725-1727`
- **谐波激励处理**: `capture.rs:1728-1732`
- **频谱推送**: `capture.rs:1959-1967`
- **RT60 计算**: `capture.rs:1366-1381`

